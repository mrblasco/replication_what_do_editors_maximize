****** Required/Recommended Stata packages *******

ssc install egenmore, replace
ssc install estout, replace
* ssc install ivreg2
ssc install ivreg29, replace
ssc install outreg, replace
ssc install outreg2, replace
ssc install ranktest, replace

** NOTICE **
* in some instanced unrecognized command:  svmat2 appears
* as usal type -- findit svmat2 -- and install the first occurence (if necessary)
